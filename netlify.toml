[build]
  command = "pnpm install --no-frozen-lockfile --no-optional && pnpm run build"
  publish = ".netlify/functions-internal"

[dev]
  command = "pnpm run dev"
  port = 5173

[build.environment]
  NODE_VERSION = "18" 