# Make sure SvelteKit-internal requests go to the function
/.netlify/* /.netlify/:splat 200

# API routes
/api/* /.netlify/functions/sveltekit-render 200

# SPA fallback - must be last
/* /index.html 200
